# React + Next.js 项目重构 - src/pages 架构

## 项目概述

本项目已重构为基于 `src/pages` 目录的现代化 React 应用，集成了状态管理、组件演示和完整的用户界面示例。

## 🏗️ 项目结构

```
src/
├── components/          # 通用组件
│   └── index.ts        # 组件导出
├── pages/              # 页面组件
│   ├── index.tsx       # 首页
│   ├── server/         # 服务器管理页面
│   │   └── index.tsx
│   ├── antd-demo/      # Ant Design 组件演示
│   │   └── index.tsx
│   ├── antdx-demo/     # Ant Design X 组件演示
│   │   └── index.tsx
│   ├── counter-demo/   # 计数器状态管理演示
│   │   └── index.tsx
│   └── profile/        # 用户资料页面
│       └── index.tsx
└── stores/             # 状态管理
    ├── index.ts        # Store 统一导出
    ├── serverStore.ts  # 服务器状态管理
    ├── counterStore.ts # 计数器状态管理
    └── userStore.ts    # 用户状态管理
```

## 🚀 快速开始

### 安装依赖
```bash
npm install
# 或
pnpm install
```

### 启动开发服务器
```bash
npm run dev
# 或
pnpm dev
```

### 构建生产版本
```bash
npm run build
# 或
pnpm build
```

## 📱 页面路由

- `/` - 首页 (功能导航和项目介绍)
- `/server` - 服务器管理页面 (状态管理示例)
- `/antd-demo` - Ant Design 组件演示
- `/antdx-demo` - Ant Design X 组件演示  
- `/counter-demo` - 计数器状态管理演示
- `/profile` - 用户资料页面 (用户状态管理)

## 🔄 状态管理

### Zustand Store 概览

项目使用 Zustand 作为状态管理解决方案，提供了三个主要 Store：

#### 1. ServerStore (`serverStore.ts`)
- **用途**: 服务器状态管理
- **状态**: 
  - `activeMenuKey` - 当前激活的菜单项
  - `collapsed` - 侧边栏折叠状态
  - `serverInfo` - 服务器信息 (CPU、内存、磁盘、运行时间)
- **操作**: 菜单切换、侧边栏折叠、信息更新

#### 2. CounterStore (`counterStore.ts`)
- **用途**: 基础计数器状态管理演示
- **状态**: `count` - 计数器的值
- **操作**: 
  - `increment()` / `decrement()` - 基础增减
  - `reset()` - 重置为 0
  - `incrementBy(amount)` / `decrementBy(amount)` - 指定数量增减

#### 3. UserStore (`userStore.ts`)
- **用途**: 用户状态管理
- **状态**: 
  - `user` - 用户信息对象
  - `isAuthenticated` - 登录状态
- **操作**: 
  - `login(userData)` - 用户登录
  - `logout()` - 用户登出
  - `updateProfile(userData)` - 更新用户资料

### 使用示例

```typescript
// 在组件中使用 Store
import { useServerStore, useCounterStore, useUserStore } from '@/stores';

function MyComponent() {
  // 使用服务器状态
  const { serverInfo, updateServerInfo } = useServerStore();
  
  // 使用计数器状态
  const { count, increment, decrement } = useCounterStore();
  
  // 使用用户状态
  const { user, isAuthenticated, login } = useUserStore();
  
  return (
    // JSX
  );
}
```

## 🎨 技术栈

- **React 19** - 前端框架
- **Next.js 16** - React 全栈框架
- **TypeScript** - 类型安全
- **Ant Design 5** - UI 组件库
- **Zustand** - 轻量级状态管理
- **TailwindCSS** - 原子化 CSS 框架
- **UnoCSS** - CSS 引擎

## 🎯 主要特性

### 1. 现代化架构
- 基于 `src/pages` 的清晰目录结构
- TypeScript 严格类型检查
- 组件化开发模式

### 2. 状态管理
- Zustand 轻量级状态管理
- TypeScript 类型支持
- 响应式状态更新

### 3. UI/UX
- Ant Design 组件库
- 响应式设计
- 现代化的用户界面

### 4. 开发体验
- 热重载开发服务器
- ESLint 代码检查
- 完整的类型提示

## 📋 开发指南

### 添加新页面
1. 在 `src/pages/` 目录下创建新的页面文件夹
2. 在文件夹内创建 `index.tsx` 文件
3. 导出默认组件

### 添加新 Store
1. 在 `src/stores/` 目录下创建新的 Store 文件
2. 使用 Zustand 创建状态管理
3. 在 `src/stores/index.ts` 中导出

### 添加通用组件
1. 在 `src/components/` 目录下创建组件文件
2. 在 `src/components/index.ts` 中导出

## 🛠️ 配置说明

### Next.js 配置
- 使用 App Router
- 支持 TypeScript
- 集成 TailwindCSS 和 UnoCSS

### Ant Design 配置
- 使用 `@ant-design/nextjs-registry`
- 支持主题定制
- 完整的组件支持

## 📝 代码规范

- 使用 TypeScript 严格模式
- 遵循 ESLint 规则
- 使用函数式组件和 Hooks
- 统一的命名规范

## 🔮 未来计划

- [ ] 添加单元测试
- [ ] 集成 E2E 测试
- [ ] 添加国际化支持
- [ ] 实现 PWA 功能
- [ ] 添加数据可视化图表
- [ ] 实现实时通信功能

---

本项目展示了现代 React 开发的最佳实践，包括状态管理、组件化开发和用户体验优化。通过清晰的项目结构和完整的状态管理示例，为后续开发提供了良好的基础。