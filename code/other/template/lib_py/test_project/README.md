# test-project

A test project to demonstrate usage of the lib-py package.

## Usage

This project demonstrates how to use the lib-py package. 

To run the test:

```bash
uv run src/test_project/test_demo.py
```

Note: Make sure you have uv installed and the lib-py package available in the parent directory.