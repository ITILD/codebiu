# 权限设计 v4 升级方案

> 现行实现见 [03-权限设计.md](./03-权限设计.md)。本文由原《03-权限设计-新版》（v4 修订设计）与《03-权限设计-新版如何实现》（差距分析与实施清单）合并而成。

# 一、v4 修订设计

**半 To C 版本（偏向个人创作者与小微企业团队）** 的知识库系统权限设计（最终版 v4）。

> **本版（v4）修订说明**（依据本文第二部分对当前代码的核对结论修订 v3）：
> 1. **纠正 v3 前提**：代码中 `is_private` 默认值已是 `true`，本版核心变更修正为"补齐私有库在**列表与详情两层**的完全隐身"，无需数据迁移。
> 2. **收敛管理员边界矛盾**（v3 中 2.1 与 3.3/5.1 三处说法冲突）：统一为"admin 穿透项目权限用于管理与审计；模型**元数据**可见全量，密钥除本人外一律脱敏，不可绑定使用他人私有模型"。
> 3. **模型范围补全**：保留部门模型，形成 公有 / 部门 / 私有 三类 scope，避免照搬 v3 丢失既有能力。
> 4. **兜底链可感知、可关闭**：私有模型失效回退公共模型必须提示，并提供用户级"不回退直接报错"开关。
> 5. **明确状态码**：私有库对非成员的**读**访问返回 404（防存在性探测），成员档位不足仍 403。
> 6. **保底规则补边界**：仅统计激活用户；部门授权来源计入统计；写明与 admin 穿透的关系。

---

# 1. 总体设计

系统权限分为两层，互不耦合：

1. **全局功能权限**（菜单/按钮/接口）
   - 使用 Casbin，控制用户能"进入哪些模块、看到哪些按钮"。
2. **项目（知识库）与数据级权限**（资源归属与可见性）
   - 知识库使用 `project_member` / `project_dept` 表控制。
   - 模型配置使用 `scope`（`public` / `dept` / `user`）+ `user_id` / `dept_id` 字段进行数据级隔离（系统公有 / 部门 / 个人私有）。

认证方式：OAuth2 表单登录 + JWT Bearer Token。**未登录用户不可访问任何接口（401）**。

---

# 2. 角色定义

## 2.1 全局角色（Casbin）

| 角色 | 角色标识 | 说明 |
|---|---|---|
| 系统管理员 | `admin` | 穿透一切全局功能与项目级权限（列表可见全量、可管理，用于系统审计与保底兜底）。模型侧：可见全部配置**元数据**（归属人/模型名/状态等），但 URL/API Key **除本人私有配置外一律脱敏**，且**不可绑定使用他人私有模型**。 |
| 普通用户 | `user` | 系统的主体。可创建私有知识库、配置个人私有模型、使用系统公有/本部门模型。 |

## 2.2 项目内角色（成员表，数值越大权限越高）

| 档位 | 角色 | 权限说明 |
|---|---|---|
| 3 | `project_admin` | 全部：删除项目、成员与部门管理、**设置公开/私有（`publish`）** |
| 2 | `project_editor` | 读 + 写：上传/修改文档、对话问答（**不可变更公开/私有状态**） |
| 1 | `project_reader` | 仅只读：查看文档、对话问答 |

---

# 3. 知识库（项目）权限设计

## 3.1 默认私有规则

知识库字段 `is_private` 默认值为 **`true`（私有/非公开）**——当前代码已如此，无需数据迁移；本版落地重点是隐身的**服务端强制**：

- **私有状态（默认）**：只有 创建者 / 直连成员 / 授权部门 的用户能看到该知识库——**列表不返回（5.1 SQL 过滤）+ 详情读访问返回 404（5.2）**，两层共同构成"完全隐身"。
- **公开状态（手动开启）**：`is_private = false` 时所有登录用户均可在列表中看到并**只读**该知识库。"设为公开/私有"是 `project_admin` 专属动作（动作码 `publish`，档位 3），editor 不可变更可见性。

## 3.2 知识库创建与授权规则

1. **创建即拥有**：新建知识库后，创建者自动成为该知识库的 `project_admin`（档位 3）。
2. **显式授权**：`project_admin` 可添加其他用户或部门，并为其分配 `admin` / `editor` / `reader` 档位。
3. **保底规则**：移除成员或降级时，必须保证该知识库至少保留一个**激活的** `project_admin`（直连成员与部门授权来源均计入统计），防止知识库成为"孤儿"。
   - 仅统计 `is_active=true` 的用户；唯一 admin 被禁用/注销同样触发保底拦截。
   - 与 admin 穿透的关系：全局 admin 的 Casbin 穿透保证系统层面永远有人可管，业务层保底仅防"业务层无人可管"，二者不互相替代。

## 3.3 知识库权限矩阵

| 角色 / 状态 | 私有知识库（默认） | 公开知识库（手动开启） |
|---|---|---|
| 系统管理员(admin) | 穿透可见、可管理（列表全量 + 内容操作，用于审计与兜底） | 穿透可见、可管理 |
| project_admin | 所有权限 | 所有权限 |
| project_editor | 读 + 写 | 读 + 写 |
| project_reader | 只读 | 只读 |
| **其他登录用户** | **不可见、不可读、不可写（列表隐身，详情 404）** | **只读（可查看/问答，不可写）** |
| 未登录用户 | 401 不可访问 | 401 不可访问 |

> 补充：项目内成员档位不足时的访问返回 **403**（例如 editor 尝试删除）；仅"私有库 + 非成员"的**读**访问返回 **404**。

---

# 4. 模型管理权限设计

模型分为 **系统公有模型 / 部门模型 / 用户私有模型** 三类，通过 `scope` + `user_id` / `dept_id` 字段隔离（v3 只有公有/私有两类，v4 补全部门模型）。

## 4.1 模型归属与可见性

| 模型类型 | 归属 | 可见性与可用性 | 密钥(URL/API Key)保护 |
|---|---|---|---|
| **系统公有模型** | `scope=public`（`user_id` 为空） | 所有登录用户可见、可用。 | 后端脱敏（`mask_secrets`），前端不可见明文。 |
| **部门模型** | `scope=dept`（`dept_id`） | 归属部门成员可见、可用。 | 后端脱敏，前端不可见明文（密钥仅系统内部调用使用）。 |
| **用户私有模型** | `scope=user`（`user_id=本人`） | **仅创建者本人**可见、可用。 | 仅本人可见明文；**admin 可见元数据，但密钥一律脱敏**（信任基石：谁也拿不走用户的 Key）。 |

## 4.2 模型操作权限矩阵

| 操作 | 系统管理员 (admin) | 普通用户 (user) |
|---|---|---|
| **新增/修改/删除** 系统公有模型 | 是 | 否 |
| **设置** 系统默认公有模型 | 是 | 否 |
| **新增/修改/删除** 个人私有模型 | 否（不可越权操作他人数据） | **是（仅限 `user_id = 自己` 的数据）** |
| **绑定使用** 他人私有模型 | **否**（admin 不放行） | **否** |
| **测试** 模型连通性 | 是（测公有/部门/自己名下配置；**不可测他人私有**） | 是（测公有/部门/自己私有） |
| **查看** 模型配置列表 | 是（**全量元数据**；他人配置的 url/api_key 一律脱敏） | 是（公有 + 本部门 + 自己私有） |

> 补充安全要求：创建/修改私有模型时校验 `base_url`（协议白名单 http/https，可选域名黑名单），并记录审计日志——私有配置不允许成为无审计的内网探测通道。

## 4.3 模型调用兜底链

当用户发起问答或向量化请求时，模型选择逻辑如下：

```text
1. 用户个人偏好绑定的模型（自己的私有 / 本部门 / 系统公有）
   ↓ (若未绑定或已失效)
2. 系统默认公有模型 (scope=public 且 is_default=true 且 is_active=true)
   ↓ (若系统未配置或已禁用)
3. 返回"无可用模型"错误（不做进一步静默兜底）
```

补充规则（v4 新增）：

- **回退可感知**：发生"绑定模型 → 默认公共模型"回退时，响应（SSE 场景在首包 meta）必须提示"绑定的模型不可用，已回退系统公共模型"；
- **回退可关闭**：提供用户级开关"绑定模型不可用时直接报错，不回退"（默认开启回退）。用户选择私有模型往往意味着"数据不经系统服务商"，静默降级会改变数据流向且用户无感知；
- v3 中的"config.yaml 兜底"仅指系统默认公共模型的 seed 来源，不作为运行时第三级兜底。

---

# 5. 开发鉴权与查询规则

## 5.1 列表查询过滤（SQL 级拦截）

用户请求知识库列表时，后端必须拼接以下可见性条件（推荐 `EXISTS` 写法）：

```sql
WHERE (
    is_private = false  -- 公开库所有登录用户可见
    OR created_by = :current_user_id  -- 自己创建的
    OR EXISTS (SELECT 1 FROM project_member pm
               WHERE pm.project_id = project.id AND pm.user_id = :current_user_id)  -- 直连成员
    OR EXISTS (SELECT 1 FROM project_dept pd
               WHERE pd.project_id = project.id AND pd.dept_id IN (:user_dept_chain))  -- 部门授权(含祖级级联)
)
```

*注 1：系统管理员（admin）跳过此过滤，看到全量列表以便系统审计（与 2.1/3.3 一致）。*
*注 2：用户无部门时必须省略部门子句，不得生成 `IN ()` 非法 SQL。*
*注 3：部门链由用户所在部门的 `ancestors` 祖级链 + 自身部门构成，每请求查询一次并在 list/count 间复用。*

## 5.2 接口鉴权顺序

```text
1. 校验 JWT 登录态（401）
2. 校验全局菜单/按钮权限（Casbin enforce，403）
3. 若是模型接口：校验数据归属（scope=public / dept 命中 / user_id==self；admin 元数据可见但密钥脱敏）
4. 若是知识库接口：校验项目级档位（max(直连, 部门) >= 动作所需档位，403）
5. 校验公开/私有状态：私有库非成员 → 列表不返回；读接口返回 404（防存在性探测）；写接口返回 403
```

## 5.3 列表返回权限位 (`my_perms`)

列表接口需为每个知识库计算并返回当前用户的操作权限位，前端据此渲染 UI：

```json
{
  "id": "101",
  "name": "我的商业计划书",
  "is_private": true,
  "my_perms": {
    "read": true,
    "upload_doc": true,
    "update": true,
    "delete": false,
    "manage_member": false
  }
}
```

> 字段映射（前后端约定）：`upload_doc` ← 后端动作 `upload`；`manage_member` ← 后端动作 `invite/remove/manage`；其余同名。分页场景需批量计算（一次取当页 project_ids 批量查成员/部门授权表），禁止逐项目 N+1 查询。

---

# 6. 最终规则总结（半 To C 版 v4）

1. **隐私优先**：知识库默认私有，未授权用户在列表中完全看不到该知识库，直接访问详情返回 404。
2. **分享机制**：仅 `project_admin` 可将知识库设为"公开"（`publish` 动作），全员可见但仅限只读。
3. **创建者即 Owner**：新建知识库自动成为 `project_admin`。
4. **三档协作**：`admin`（管理）/ `editor`（写文档）/ `reader`（只读）。
5. **模型三类隔离**：公有 / 部门 / 个人私有（自己的 API Key）；admin 可见全量**元数据**用于运维，但密钥除本人私有配置外一律脱敏，且**不可绑定使用他人私有模型**。
6. **模型兜底**：优先用户绑定模型，其次系统默认公共模型；回退必须可感知，并提供"不回退直接报错"的用户开关。
7. **管理员边界**：admin 穿透项目权限用于管理与审计（列表全量、可管理）；模型侧只多"元数据可见"，不多"密钥可见"与"他人私有模型可用"。
8. **保底机制**：任何知识库必须至少保留一个**激活的** `project_admin`（直连或部门授权来源均计入）。
9. **未登录拦截**：系统不对外网匿名开放，所有接口必须携带 JWT（401 拦截）。

---

# 二、现状差距与实现清单


> 上游文档：
> - 现状设计：`./03-权限设计.md`（描述当前代码的权限模型）
> - 新版需求（v4 修订版）：`本文第一部分`——v3 的设计矛盾（admin 边界三处冲突、"改默认值"过时前提、403/404 未定等）已按本文第一版核对结论在 v4 中收敛定案，原"设计问题清单"相应转为实施注意事项。
> - 本文：以**当前代码实际实现**为基线的差距分析与实施清单。
>
> 基线代码核对范围：`code/backend/server_py`（核对日期 2026-09-09）。

---

## 1. 结论速览

新版 v4 可验收条款核对结果：

| # | 新版(v4)条款 | 代码现状 | 结论 |
|---|---------|---------|------|
| 1 | 知识库默认私有（`is_private` 默认 true） | `module_rag/do/project.py` 中 `is_private: bool = Field(default=True)` | ✅ 已实现，无需数据迁移 |
| 2 | 私有库**列表**完全隐身 | `GET /rag/projects/list` 仅 casbin 校验，SQL 无用户可见性过滤 | ❌ **P0 最大差距**：有 `rag:project:read` 的用户可列出他人私有库 |
| 3 | 私有库**详情**读访问 404 | `enforce_project_permission` 统一抛 403 | ❌ P1 |
| 4 | 创建即拥有（creator = project_admin） | `module_rag/service/project.py` `add()` 写入创建者 `PROJECT_ADMIN` 成员记录 | ✅ |
| 5 | 三档位 + max(直连,部门) + 部门级联 | `module_rag/dependencies/permission.py`（ACTION_LEVELS / get_dept_role_level） | ✅ |
| 6 | `publish` 动作仅 project_admin | 项目 update 走 `("project","update")` 档位 2，**editor 可把私有库切成公开** | ❌ P0 |
| 7 | 保底：至少一个**激活的** project_admin（部门来源计入） | `module_rag/service/project_member.py` delete/update、`project_dept` 写操作均无校验 | ❌ P1 |
| 8 | 模型三类 scope 隔离（public/dept/user） | `module_ai/do/model_config.py` `ModelScope` + `user_id`/`dept_id` | ✅ |
| 9 | admin 模型边界：元数据全量可见、密钥除本人外脱敏、不可绑定使用他人私有模型 | 三处相反：`mask_secrets` 对 admin 豁免（明文）、`_validate_model_access` 对 admin 放行（可用他人私有）；`_visibility_condition` admin 全量可见（元数据侧恰好符合 v4） | ❌ P2（v4 已定案） |
| 10 | 兜底链可感知、可关闭 | `module_rag/service/user_model.py` `resolve_model_id()` 已实现"绑定→归属校验→默认公共模型"链路，但**静默回退**、无用户开关 | ❌ P1 |
| 11 | `my_perms` 权限位 | `ProjectResponse` 无此字段 | ❌ P1 |
| 12 | 未登录 401 | 全部 rag 路由经 `get_current_user_id` / `require_permission` | ✅ |

---

## 2. 现状已满足的新版要求（无需改动）

1. **两级权限体系**：系统级 Casbin 域隔离 RBAC + 项目级成员档位，项目级不走 Casbin 项目域（`module_rag/dependencies/permission.py`）。
2. **默认私有**：`Project/ProjectCreate/ProjectResponse` 的 `is_private` 均默认 `True`。
3. **创建即拥有**：`module_rag/controller/project.py` `create_project` → `service/project.py:105-135` 填充 `created_by` 并写 `role=PROJECT_ADMIN` 成员记录。
4. **三档协作与级联**：`project_reader(1)/project_editor(2)/project_admin(3)`，生效档位 = max(直连, 部门授权)，部门链由 `ancestors` 展开。
5. **公开库登录用户只读**：`check_project_permission` 中 `act == "read"` 且 `not project.is_private` 放行。
6. **模型三类隔离与防越权绑定**：`mask_secrets` 对"非本人创建"清空 `url/api_key`；`_validate_model_access` 防止绑定他人模型。
7. **模型兜底链主体**：`resolve_model_id` = 用户绑定（chat/embedding/rerank）→ 使用前归属校验 → 回退 `scope=public 且 is_default=True 且 is_active=True` 的默认公共模型；`get_missing_model_types` 做任务派发前预检。
8. **未登录 401**：全路由认证依赖覆盖。

---

## 3. 差距与实现方案

### 3.1 [P0] 项目列表可见性过滤（私有库列表隐身）

**现状**：`module_rag/dao/project.py` 的 `list_paged/count` 只有 `name/kb_category/is_private` 过滤；`controller/project.py` 的 `/list` 只要求 `rag:project:read`（普通用户默认策略含此权限码）。

**方案**：DAO 增加按"查看者"的可见性条件（对齐 v4 5.1 的 EXISTS 写法），controller 传入当前用户；admin 跳过过滤：

```python
# module_rag/dao/project.py — list_paged/count 增加 viewer_id 参数
from sqlmodel import or_, exists, select, false

def _visibility_condition(self, viewer_id: str, dept_chain: list[str]):
    """私有库仅 创建者/直连成员/部门授权 可见, 公开库所有登录用户可见(v4 5.1)"""
    member_exists = exists(
        select(1).where(
            ProjectMember.project_id == Project.id,
            ProjectMember.user_id == viewer_id,
        )
    )
    # 用户无部门时不得生成 in_([]) 非法 SQL(v4 5.1 注 2)
    dept_exists = (
        exists(select(1).where(
            ProjectDept.project_id == Project.id,
            ProjectDept.dept_id.in_(dept_chain),
        ))
        if dept_chain else false()
    )
    return or_(
        Project.is_private == False,      # noqa: E712 公开库
        Project.created_by == viewer_id,  # 自己创建的
        member_exists,
        dept_exists,
    )
```

```python
# module_rag/controller/project.py — /list 改为认证依赖 + admin 跳过
@router.get("/list", summary="分页查询项目列表(私有库仅授权人可见)", ...)
async def list_projects(
    ...,
    current_user_id: str = Depends(get_current_user_id),
    _perm=Depends(require_permission("rag", "project", "read")),
):
    # admin 跳过可见性过滤(系统审计, v4 5.1 注 1)
    viewer_id = None if await is_global_admin(current_user_id) else current_user_id
    return await service.list_paged(..., viewer_id=viewer_id)
```

实现要点：

- 部门链 `get_user_dept_chain(user_id)` 每请求只查一次，list/count 复用（v4 5.1 注 3）；
- 提取公共函数 `is_global_admin(user_id)`（封装 `enforcer.has_grouping_policy(user_id, "admin", "*")`，当前在 `module_rag/dependencies/permission.py:85` 与 `module_rag/service/user_model.py:59` 重复两处）；
- 已有的成员视角接口 `ProjectMemberService.list_my_projects` 保留，二者互补：`/list` = 可见列表，`/my` = 成员列表。

### 3.2 [P0] 公开/私有切换需 project_admin 档位（publish 动作）

**现状**：`update_project` 走 `("project","update")` → 档位 2，editor 可将私有库切为公开（数据泄露入口）。

**方案**：新增档位 3 动作 `publish`（v4 2.2/3.1），service 层对 `is_private` 变更单独校验：

```python
# module_rag/dependencies/permission.py
ACTION_LEVELS: dict[str, int] = {
    ...,
    "publish": 3,  # 设置公开/私有: 仅 project_admin(v4 3.1)
}

# module_rag/service/project.py — update() 内
existing = await self.get(project_id)
if existing and project.is_private is not None and project.is_private != existing.is_private:
    await enforce_project_permission(current_user_id, project_id, "project", "publish")
```

注意：`enforce_project_permission` 需要 `current_user_id`，controller 需把它传进 service（当前 `update` 未传）。

### 3.3 [P1] 私有库详情读访问 404 化（防存在性探测）

**现状**：`enforce_project_permission` 对无权限统一抛 403。

**方案**（v4 3.3 补充/5.2 第 5 步已定案）：仅 `read` 动作 + 私有库 + 非成员（档位 0）时改抛 `NotFoundError`（404）；已登录成员档位不足仍 403，写动作仍 403。改造集中在 `module_rag/dependencies/permission.py` 的 `check/enforce` 两函数。顺带按项目异常体系规约把 403 从裸 `HTTPException` 改为 `ForbiddenError`（`common.utils.fastapiEX.exceptions`），保持既有测试语义一致。

### 3.4 [P1] 列表返回 `my_perms` 权限位

**方案**（字段与映射对齐 v4 5.3）：

```python
class ProjectMyPerms(SQLModel):
    """当前用户对单个知识库的操作权限位(前端按位渲染按钮)"""
    read: bool = False            # 档位>=1
    upload_doc: bool = False      # 映射动作 upload, 档位>=2
    update: bool = False          # 档位>=2
    delete: bool = False          # 档位>=3
    manage_member: bool = False   # 映射动作 invite/remove/manage, 档位>=3
```

计算方式：分页场景禁止逐项目调 `check_project_permission`（N+1）。一次性取当页 `project_ids` → 批量查 `project_member(user_id, project_id in ids)` 与 `project_dept(dept_id in dept_chain, project_id in ids)` → 内存取 max 档位 → 映射动作位；admin 全 True。详情接口复用同一函数。

### 3.5 [P1] 至少保留一个激活的 project_admin 保底

**现状**：成员移除/降级、部门授权增删改均无 admin 数量校验。

**方案**（v4 3.2 规则 3）：成员与部门授权两条线的写操作前统一校验：

```python
async def _assert_admin_remaining(
    self, project_id: str,
    exclude_member_id: str | None = None,   # update 降级场景排除自身旧档位
) -> None:
    """移除/降级后必须仍有激活的 project_admin(直连成员或部门授权来源均计入, v4 3.2)"""
    direct = await self.member_dao.count_admins(
        project_id, exclude_member_id=exclude_member_id, active_only=True  # join user 表过滤 is_active
    )
    via_dept = await self.dept_dao.count_admins(project_id)
    if direct + via_dept == 0:
        raise ConflictError("必须至少保留一个项目管理员(project_admin)")
```

覆盖点：`member.delete`（目标是 admin 时）、`member.update`（降级 admin 时）、`project_dept` 移除/降级 admin 授权时。边界处理见 §4.3。

### 3.6 [P1] 兜底链可感知 + 用户级回退开关

**现状**：`resolve_model_id` 在绑定失效时静默回退默认公共模型，用户无感知（v4 4.3 认定为隐私问题：数据流向改变）。

**方案**：

```python
# module_rag/service/user_model.py — 解析结果带回退标记
from typing import NamedTuple

class ResolvedModel(NamedTuple):
    model_id: str | None
    fallback_used: bool = False  # True=绑定失效/未绑定, 已回退默认公共模型

async def resolve_model(self, user_id: str, model_type: ModelType) -> ResolvedModel:
    """绑定 → 归属校验 → (开关允许时)回退默认公共模型; 结果标记是否发生回退"""
    binding_id = await self._get_binding_id(user_id, model_type)
    if binding_id:
        try:
            await self._validate_model_access(binding_id, user_id)
            return ResolvedModel(model_id=binding_id)
        except (ValueError, NotFoundError):
            pass  # 绑定失效, 走回退
    if await self._fallback_disabled(user_id):  # 用户级开关: 不回退直接报错
        return ResolvedModel(model_id=None)
    fallback_id = await self._get_fallback_model_id(model_type)
    return ResolvedModel(model_id=fallback_id, fallback_used=fallback_id is not None)
```

- **开关存储**：`user_model` 表加 `fallback_disabled: bool = Field(default=False)`，按项目惯例用启动钩子补列（参照 `module_ai/config/server.py` 的 `ensure_model_config_scope_columns` 模式）；
- **可感知**：调用方（chat 流式/非流式、向量化）依据 `fallback_used` 在 SSE 首包 meta 或响应 meta 提示"绑定的模型不可用，已回退系统公共模型"；开关关闭且无可用模型时抛 `BusinessError`（400）而非静默换模型。

### 3.7 [P2] 模型 admin 边界（v4 已定案，按三处差异落地）

v4 4.1/4.2 定案为"元数据全量可见、密钥除本人外脱敏、不可用他人私有"，与现状三处差异：

| 落点 | 现状 | 改为 |
|------|------|------|
| `module_ai/dao/model_config.py` `_visibility_condition` | admin 返回 None（全量可见） | **保持不变**（恰好符合 v4"查看列表=全量元数据"） |
| `module_ai/service/model_config.py` `mask_secrets` | `if configs is None or is_admin: return`（admin 全明文） | 删除 `or is_admin` 豁免：仅 `scope=user 且 user_id==本人` 保留明文，公共/部门/他人私有一律脱敏（含 admin） |
| `module_rag/service/user_model.py` `_validate_model_access` | admin 放行（可绑定使用他人私有） | 删除 admin 放行两行（与 v4 4.2"绑定使用他人私有模型=否"一致） |

补充（v4 4.2 安全要求）：私有模型 create/update 校验 `base_url`（协议白名单 http/https + 可选域名黑名单，黑名单走配置），写操作记审计日志（logging 起步即可）。

---

## 4. 实施注意事项

1. **`my_perms` 命名映射**：对外字段按 v4 5.3（`upload_doc`/`manage_member`），内部映射到 `ACTION_LEVELS` 的 `upload`/`invite`/`remove`/`manage`，映射关系写入接口文档，避免前后端各叫一套。
2. **SQL 细节**：可见性用 `EXISTS` 而非 `IN (SELECT ...)`；用户无部门时用 `false()` 占位，禁止 `in_([])`；部门链一次查询复用。
3. **保底边界**：`count_admins` 需 join `user` 表过滤 `is_active=true`（唯一 admin 被禁用也拦截）；部门授权来源计入统计；admin 的 Casbin 穿透保证系统层面兜底，业务保底仅防"业务层孤儿"（v4 3.2 已写明，不要过度设计）。
4. **`is_global_admin` 提取**：公共函数放 `module_authorization` 侧（或 `common`），`module_rag` 两处引用改为复用。
5. **异常体系统一**：项目级 403/404 改用 `ForbiddenError`/`NotFoundError`，controller 不再出现裸 `HTTPException` 透传；改动后核对现有 tests 的断言（403/404 状态码不变，仅异常载体变化）。
6. **前端配套**：项目列表按 `my_perms` 渲染操作按钮（无权限入口直接隐藏）；项目设置页"公开/私有"开关仅对具备 `manage_member`（或 `delete`）位的用户可编辑；模型页私有模型文案与"回退提示"展示；用户设置页新增"模型回退开关"。

---

## 5. 实施顺序

| 阶段 | 内容 | 主要落点 |
|------|------|----------|
| P0 | 列表可见性过滤（§3.1） | `module_rag/dao/project.py`、`service/project.py`、`controller/project.py` |
| P0 | publish 动作档位提升（§3.2） | `dependencies/permission.py`、`service/project.py` |
| P1 | 私有库读 404 化（§3.3） | `module_rag/dependencies/permission.py` |
| P1 | `my_perms` 权限位（§3.4） | `module_rag/do/project.py`、`service/project.py` |
| P1 | 最后一个激活 admin 保底（§3.5） | `service/project_member.py`、`service/project_dept.py`、两个 DAO 计数方法 |
| P1 | 兜底链可感知 + 回退开关（§3.6） | `module_rag/service/user_model.py`、`do/user_model.py`（补列钩子）、chat/向量化调用方 |
| P2 | 模型 admin 边界 + base_url 校验（§3.7） | `module_ai/service/model_config.py`、`module_rag/service/user_model.py` |
| 配套 | 前端（§4.6） | `src/modules/rag/`、`src/modules/ai/` |

依赖关系：§3.1/§3.3 依赖 `is_global_admin` 提取；§3.4 依赖 §3.1 的部门链查询；§3.7 的 `_validate_model_access` 改动需与 §3.6 的回退逻辑同一批联调（admin 放行删除后，admin 绑定他人私有模型将走回退路径）。

---

## 6. 测试要求（按项目规约）

后端标准测试落在 `code/backend/server_py/tests`，零外部依赖，鉴权 fixture 携带 token。重点用例：

- **列表隐身**：普通用户 `/list` 不含他人私有库；分别构造 创建者命中 / 直连成员命中 / 部门授权命中（含祖级级联） / 公开库全员可见 各一例；admin 列表返回全量；无部门用户不产生非法 SQL。
- **publish 档位**：editor 改 `is_private` 被拒（403）；project_admin 改成功（204）。
- **404/403 语义**：非成员 GET 私有库 404；成员档位不足 403；公开库非成员 GET 200。
- **保底**：移除/降级最后一个激活 admin 被拒（409）；部门授权来源可满足保底；唯一 admin 被禁用后再移除其他成员触发拦截。
- **my_perms**：各档位成员的权限位与矩阵一致；admin 全 True。
- **模型边界**：非本人（含 admin）查看模型 url/api_key 已脱敏；本人私有模型明文可见；admin 绑定他人私有模型被拒（走回退路径）；`base_url` 非法协议返回 400。
- **兜底链**：绑定失效回退时 `fallback_used=true` 且响应含提示；开关关闭时绑定失效返回 400 不回退；无默认公共模型时返回"无可用模型"。
